{% extends 'AppBundle::base.html.twig' %}
{% block stylesheet %}
  <link rel="stylesheet" href="{{ asset('/css/index.css') }}">
  <link href='https://fonts.googleapis.com/css?family=Shadows+Into+Light+Two' rel='stylesheet' type='text/css'>
{% endblock %}
{% block body %}
<nav class="navbar">
  <ul class="nav">
    <li class="nav-item header-title pull-left">
      <h1>Hello @{{ app.user.username }} {{ random(['＼(・｀(ｪ)・)/', '(*ノ・ω・）', 'o (◡‿◡✿)', 'ヽ(*・ω・)ﾉ']) }}</h1>
    </li>
    <li class="nav-item pull-right">
      <!-- React <MenuComponent /> render position -->
    </li>
  </ul>
</nav>
<main id="container" class="container">
  <!-- React <TimelineComponent /> render position -->
</main>
{% endblock %}
{% block javascript %}
<script type="text/javascript">
  var pastday_json_url = "{{ path('pastday_json') }}";
  var timeline_json = eval({{ timeline|json_encode()|raw }});
  var past_timeline_date_list = eval({{ past_timeline_date_list|json_encode()|raw }});
</script>
<script src="{{ asset('/js/index.js') }}"></script>
{% endblock %}
